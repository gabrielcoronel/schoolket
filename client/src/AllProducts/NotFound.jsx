import { FullScreenMessage } from '../general-components';

const NotFound = () => {
  return (
    <FullScreenMessage message="No encontramos lo que estabas buscando" />
  );
};

export default NotFound;